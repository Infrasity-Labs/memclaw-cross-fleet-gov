version: "3.9"

services:
  governance-runtime:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    stdin_open: true
    tty: true
    env_file: ../.env
    volumes:
      - ../logs:/app/logs
